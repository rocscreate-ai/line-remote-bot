require('dotenv').config();     // ← 放在檔案最前面
const express = require('express');
const line = require('@line/bot-sdk');
const axios = require('axios');

const {
  PORT = 3000,
  LINE_ACCESS_TOKEN,
  LINE_CHANNEL_SECRET,
  DEVICE_BASE_URL,
} = process.env;

{
  "name": "line-remote-bot",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": { "start": "node index.js" },
  "dependencies": {
    "@line/bot-sdk": "^7.5.2",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "axios": "^1.6.8"
  }
}
